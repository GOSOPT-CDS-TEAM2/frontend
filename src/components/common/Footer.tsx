import styled from 'styled-components';

const Footer = () => {
  return (
    <St.FooterContainer>
        
    </St.FooterContainer>
  );
};

export default Footer;

const St = {
  FooterContainer: styled.div`
    `,
};
